<?xml version="1.0" encoding="utf-8"?>
<androidx.appcompat.widget.LinearLayoutCompat xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?colorPrimary"
    android:fitsSystemWindows="true"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/imageViewDrawerHeader"
        android:layout_width="@dimen/profile_picture_size"
        android:layout_height="@dimen/profile_picture_size"
        android:layout_marginStart="24dp"
        android:layout_marginTop="24dp"
        android:contentDescription="@string/drawer_header_background"
        android:src="@drawable/ic_shield" />

    <TextView
        android:id="@+id/textViewDrawerHeaderName"
        style="@style/Theme.SuperheroCards.Drawer.Header.Title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="24dp"
        android:layout_marginTop="24dp"
        android:layout_marginEnd="24dp"
        tools:text="@string/fake_username" />

    <TextView
        android:id="@+id/textViewDrawerHeaderId"
        style="@style/Theme.SuperheroCards.Drawer.Header.Subtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="24dp"
        tools:text="@string/fake_user_id" />

</androidx.appcompat.widget.LinearLayoutCompat>

